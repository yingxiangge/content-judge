# -*- coding: utf-8 -*-
"""内容潜力打分 —— 判「这个题/这条事实做成内容有没有传播结构」。

🔴 **与本包其余 spec 的根本区别**：那些评的是**成品**（口播写得好不好），
这个评的是**成品之前的原料**（选题值不值得做、事实该用哪几条）。

**定位：淘汰器，不是预测器。**（2026-09-01 设计定稿）

  「系统应该做到的不是预测爆款，而是把明显缺乏传播条件的题先淘汰。」

这一条决定了下面所有设计：**阈值宽松 · 不追求精确分 · 不需要校准**。
一旦当成预测器用，就会掉回「攒播放数据回归权重」那条路 —— 而 300 播放/条
的体量，完播率的噪声比任何效应都大，那个回归**在数学上就跑不出显著结果**。

---

## 两层结构：硬门在前，打分只管排序

**第一层 · 三道硬门（全过才入池）**

  · `G1` 账户动作     —— 问题里有观众的动作或持仓关系
  · `G2` 二级市场标的 —— 股票/指数/基金/转债/期权，宏观与国际局势本身不算
  · `G4` 内容张力     —— 存在明确的「预期 vs 结果」，且必须写出 expected / test

🔴 **2026-09-06 改**：原设计是「A 警告 / B 打脸 / C 反常识 三选一」，实测
34 条过 15 条（44%）—— 对一个淘汰器明显偏松。病根是**模型能自我欺骗**：
任何题都能说成「提醒投资者注意风险」，于是 A 恒真。
⇒ 改成必须**写出**「观众原来相信什么 / 历史可能推翻它什么」两句话，
写不出就是没张力。**用「必须产出具体内容」防自欺，比加词表管用。**
原 A/B/C 降级为 `hook_type` 标注（warning / debunk / counterintuitive），
只描述张力的类型，不再承担淘汰职责。

⚠️ **G3（可不可验证）不在本 spec** —— 那是 `topic_gate` 用代码查
testable 四要素的事。结构检查交给模型只会引入抖动，还多花一次调用。

🔴 **淘汰只由硬门决定，不由分数决定**：82.6 分与 79.3 分的差别是噪声，
而「有没有张力」是事实。让分数承担淘汰职责 ＝ 拿噪声做决策。

**第二层 · 三维打分（只在过闸的候选之间排序）**

理论依据：Berger & Milkman (2012, JMR) 对《纽约时报》三个月全部文章的分析 +
实验验证 —— **高唤醒情绪（敬畏/惊讶/愤怒/焦虑）显著提升传播**，
且「有用 / 有趣 / 令人惊讶」本身与传播正相关。

⚠️ **为什么是三维不是六维**（Relevance/Curiosity/Conflict/Emotional/Utility/
Specificity）：那六个不正交。一条「越跌越买真的安全吗」，Conflict 高的同时
Curiosity 必然高、Emotional 必然高 —— 模型会打出高度相关的三个分，
加权等于把同一个信号数了三遍，权重随之失去意义。⇒ 合并成三个正交维度：

| 维度 | 吃掉原来的 | 问的是 |
|---|---|---|
| Relevance | Relevance | 关我什么事？我的钱会怎样 |
| Tension   | Conflict + Curiosity + Emotional | 跟我想的不一样吗？信息缺口在哪 |
| Utility   | Utility + Specificity | 看完我能拿走什么 |

⚠️ **权重首版是拍的**（35/40/25），照 `journal 2026-08-30` 那条规矩如实标注。
但**它不重要**：分数只排序不淘汰，而排序对权重不敏感。**不许凭手感调**。
"""
from __future__ import annotations

# 张力类型标注。🔴 **只描述类型，不承担淘汰职责**（淘汰看三道硬门是否全过）。
HOOK_TYPES = {
    "warning":          "警告 —— 观众或其家人可能正在这么亏钱",
    "debunk":           "打脸 —— 大家普遍相信的事，数据不支持",
    "counterintuitive": "反常识 —— 看起来最安全/最合理的做法，结果相反",
}
# 三道硬门的字段名，与 spec 输出的 gate 对象一一对应。**全 True 才算过。**
GATE_KEYS = ("account_action", "secondary_market", "content_hook")

# 四维权重（受众广度 30% / 账户关联 25% / 认知冲突 25% / 决策增量 20%）
WEIGHTS = {"audience": 30, "relevance": 25, "tension": 25, "utility": 20}

ROLE = """你是短视频内容策划，负责判断一个选题做成 20~30 秒竖屏视频后的传播潜力。
面向人群：普通证券投资者。
你只评估内容的传播结构：受众是否有理由停下来、看完并产生认同。"""

GATE_SPEC = """## 第一层：硬门（三条必须全部满足，缺一条淘汰）

**G1 账户动作**
问题必须包含具体的交易操作、仓位去留或盈亏处置动作（包括特定交易动作在特定情境下的历史表现）。
排除一切缺少明确账户决策动作主体的被动事实陈述与背景描述。

**G2 二级市场标的**
标的必须属于可公开交易的证券与金融衍生资产。
标的必须具备公开连续交易报价与交割属性；排除一切无标准化交易代码的非交易标的。

**G3 内容张力**
必须存在明确的预期认知冲突：大众普遍存在直觉共识，而历史客观规律可能呈现相反或显著偏离的结果。
必须明确输出两项推演要素：
  expected  普遍直觉假设与主流共识
  test      客观历史规律可能给出的反差事实
若无法清晰定义明确的直觉共识，判定为 G3 不合格；泛化的泛金融风险提示视为未定义 expected。

按张力类型标注 hook_type：
  warning          对常见交易盲区与潜在损失风险的客观揭示
  debunk           大众普遍持有的直觉共识与历史统计事实背离
  counterintuitive 直觉上安全或合理的操作模式在历史上出现相反表现

若任一条件不满足，输出字段 gate 置 null；全部满足则输出对应对象。"""

DIM_SPEC = """## 第二层：四维打分（0-10 分）

- **Audience 受众广度**（0-10）
  评估该议题在全市场投资者中的持仓基数与关注规模。
  · 9-10：全市场普适级系统性议题，或覆盖全域投资者的核心基石资产与全市场通用规则。绝大多数普通投资者账户均有直接持仓或直接感知。
  · 7-8：跨行业的大类配置资产或全市场普遍关注的系统性主线，具备跨板块联动影响，但非全域直接底仓。
  · 5-6：单一垂直行业板块、细分大宗商品或局部产业链节点。无论短期舆论热度高低，持仓均仅局限于垂直赛道客群，不具备跨板块普适性。
  · 3-4：冷门细分板块、非主流产业链环节或小众投资品种。
  · 0-2：生僻冷门细分品种、高度专业化窄门链条，仅极少数专业从业者关注。

- **Relevance 账户关联**（0-10）
  评估议题能否直接映射至投资者的持仓标的、交易决策或盈亏处置。
  · 9-10：直接映射到具体且普遍的账户持仓动作与盈亏处置决策。
  · 5-6：涉及板块与行情波动，需多层逻辑推导才能间接关联到个人账户。
  · 0-2：宏观或高维抽象视角，与个体微观执行动作完全脱节。

- **Tension 认知冲突**（0-10）
  评估是否存在认知反差与信息缺口。
  · 9-10：明确推翻大众普遍相信的直觉共识，历史数据具备强烈反差对比。
  · 5-6：存在一定悬念但最终结论基本符合常理预期。
  · 0-2：结论完全符合直觉常识，毫无反差与意外感。

- **Utility 决策增量**（0-10）
  评估内容能否提供可量化、可验证的历史规律与统计基准。
  · 9-10：输出明确的样本区间、胜率概率或条件收益等客观统计事实。
  · 5-6：仅能提供定性的因果推演，缺乏精确量化的统计边界。
  · 0-2：充斥情绪宣泄或毫无事实依据的主观预测，无信息增量。"""

OUTPUT_JSON = """## 输出格式

只输出合法 JSON，包裹在 ```json 与 ``` 之间。逐条对应输入的编号：

{
  "items": [
    {"id": 1,
     "gate": {"account_action": true, "secondary_market": true,
              "content_hook": true, "hook_type": "debunk"},
     "expected": "普遍直觉假设与主流共识",
     "test": "客观历史规律可能给出的反差事实",
     "audience": 9, "relevance": 8, "tension": 7, "utility": 8,
     "why": "一句话说明过闸理由与最强维度"},
    {"id": 2, "gate": null,
     "expected": "", "test": "",
     "audience": 2, "relevance": 3, "tension": 2, "utility": 4,
     "why": "未通过硬门编号及缺失核心要素说明"}
  ]
}

四维分均为 0-10 的整数。严禁输出总分。
gate 为 null 时 why 必须指明未满足的硬门编号。
必须逐条给出，输入多少条即输出多少条，不省略、不合并。
不要输出任何解释说明、前言或额外文本。"""


def render_batch(items: list[dict]) -> str:
    """待评条目 → user 消息。**批量评而不是逐条**。

    🔴 逐条评有两个问题：调用次数 ×N；且模型看不到横向对比，
    打出来的分在条目之间不可比 —— 而我们要的恰恰是**排序**。
    """
    lines = ["【待评选题】", ""]
    for n, it in enumerate(items, 1):
        lines.append(f"{n}. {it.get('name') or it.get('title', '')}")
        src = (it.get("event") or it.get("from") or it.get("why") or "").strip()
        if src:
            lines.append(f"   事实边界：{src}")
        foc = (it.get("audience_focus") or "").strip()
        if foc:
            lines.append(f"   散户关切：{foc}")
        ten = (it.get("hook_tension") or it.get("testable") or "").strip()
        if ten:
            lines.append(f"   认知张力：{ten}")
    return "\n".join(lines)


SYSTEM_BLOCKS = (ROLE, GATE_SPEC, DIM_SPEC, OUTPUT_JSON)
